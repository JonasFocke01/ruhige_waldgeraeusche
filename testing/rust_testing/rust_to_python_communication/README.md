Command to run the thing is "sudo -E /home/pi/.cargo/bin/cargo run"

command to run python script standallone: "sudo python3 main.py SOMETHING" (SOMETHING, so that argv is > 1, so it doesnt matter what exactly)