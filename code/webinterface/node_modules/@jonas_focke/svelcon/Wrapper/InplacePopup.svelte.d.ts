import { SvelteComponentTyped } from "svelte";
declare const __propDef: {
    props: {
        animationStatus?: string | undefined;
        tailwindClasses?: string | undefined;
        padding?: "" | `p-${number}` | `pb-${number}` | `pt-${number}` | `pr-${number}` | `pl-${number}` | `px-${number}` | `py-${number}` | `-p-${number}` | `-pb-${number}` | `-pt-${number}` | `-pr-${number}` | `-pl-${number}` | `-px-${number}` | `-py-${number}` | undefined;
        rounded?: boolean | undefined;
        InFlyOptions?: {
            duration: number;
            y: number;
        } | undefined;
        OutFlyOptions?: {
            duration: number;
            y: number;
        } | undefined;
    };
    events: {
        outsideClick: CustomEvent<any>;
    } & {
        [evt: string]: CustomEvent<any>;
    };
    slots: {
        default: {};
    };
};
export declare type InplacePopupProps = typeof __propDef.props;
export declare type InplacePopupEvents = typeof __propDef.events;
export declare type InplacePopupSlots = typeof __propDef.slots;
/**
 * a basic popup
 *
 * **slot**
 * * HTML
 * * default: !NO DEFAULT!
 * * here can live anything you want!
 *
 * **InFlyOptions**
 * * SvelteFlyOptions
 * * default: { duration: 200, y: -50 }
 * * how should the menu fly in
 * * if set to { duration: 0 } the menu will not fly in
 *
 * **OutFlyOptions**
 * * SvetleFlyOptions
 * * default: { duration: 200, y: -50 }
 * * how should the menu fly out
 * * if set to { duration: 0 } the menu will not fly out
 *
 * **animationStatus**
 * * open | closed | opening | closing
 * * the current animation status of the menu
 * * any changes from outside will be ignored and be overwritten
 *
 * **tailwindClasses**
 * * string
 * * default: ''
 * * more control over the menu position
 *
 * **on:outsideClick**
 * * function
 * * default: () => {}
 * * will be called when the user clicks outside the popup
 */
export default class InplacePopup extends SvelteComponentTyped<InplacePopupProps, InplacePopupEvents, InplacePopupSlots> {
}
export {};
